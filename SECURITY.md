# Security

This project is designed for local use first.

- Keep the broker bound to 127.0.0.1 unless you have added a real network security model.
- Do not commit `.token`, SQLite databases, logs, API keys, or private message history.
- Agent CLIs may have their own authentication. Follow each provider's security guidance.
- If you find a vulnerability, open a private security advisory or contact the maintainer.
